<?php

/**
 * @file
 * Main code for Socrata Filter module.
 */

/**
 * Implements hook_theme().
 */
function socrata_filter_theme($existing, $type, $theme, $path) {
  $items = [];

  $items['socrata_filter'] = [
    'variables' => [
      'source' => NULL,
      'embed_url' => NULL,
      'width' => NULL,
      'height' => NULL,
    ],
  ];

  return $items;
}
